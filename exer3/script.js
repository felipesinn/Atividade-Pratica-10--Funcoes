function verificarImparPar(num) {
    console.log(num);
    const resultado = num % 2 === 0 ? 'PAR' : 'ÍMPAR';
    console.log(resultado);
  }
  
  verificarImparPar(8); // Output: 8 PAR
  verificarImparPar(7); // Output: 7 ÍMPAR
  
  
  
  

// 3. Crie uma função que recebe por parâmetro um valor inteiro e
// positivo e retorne verdadeiro se o número for primo ou falso se não
// for.